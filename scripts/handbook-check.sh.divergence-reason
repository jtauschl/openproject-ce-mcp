# Documented divergence for scripts/handbook-check.sh, per this project's copy of
# handbook-check.sh's own check_copied_script_drift() header comment.
#
# Reason: the local copy sets SW_DEV_HANDBOOK_DOC_REF's fallback default to this
# project's actual pinned tag (v0.10.5) instead of the upstream template's
# placeholder "REPLACE_WITH_YOUR_PINNED_SW_DEV_HANDBOOK_TAG" — this is the one
# substitution every consuming project is expected to make on copy, not an
# unintended local change. Expected to be re-documented (new hashes) every time
# the pin is bumped, since the local fallback value itself changes too.
local_sha256_at_documentation: 94701e35d458cfb0fae7f251cd94185688f8df6b99b9e4e96c26771ad9cb9cf8
upstream_sha256_at_documentation: 18368de7df64e6bf1c9ddbe2b36d0d8bb56d566138430663857aedbc756922fc
